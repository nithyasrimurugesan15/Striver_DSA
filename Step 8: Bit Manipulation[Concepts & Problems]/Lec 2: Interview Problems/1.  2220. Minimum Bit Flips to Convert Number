https://leetcode.com/problems/minimum-bit-flips-to-convert-number/description/

time o(log n) and space o(1)

class Solution {
    public int minBitFlips(int start, int goal) {
        int res=start^goal;
        int c=0;
        while(res!=0)
        {
            c+=res&1;    ------------->  if((res&1)==1) {
            res>>=1; // res/=2            c++;   }        
     }
        return c;
    }
}

ANOTHER APPRAOCH : PREFER

// Brain Kernighan’s method works :

time O(k) ≤ O(log N) and space o(1)

class Solution {
    public int minBitFlips(int start, int goal) {
        int res=start^goal;
        int c=0;
        while(res!=0)
        {
           res&=(res-1);
           c++;
        }
        return c;
    }
}
