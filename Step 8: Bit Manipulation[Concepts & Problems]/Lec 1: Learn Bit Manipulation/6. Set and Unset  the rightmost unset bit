T.C : O(1)
S.C : O(1)
 
Set rightmost unset bit:  chck till rightmost 1 and right side as changes to 1  -> n | (n-1)
Set rightmost set bit: turn the 1st rightmost 0 into 1 -> n | (n+1)
Unset rightmost unset bit:  chck till rightmost 0 and right side as changes to 0  -> n & (n+1)
Set lsb: chk lsb 0 to 1 and 1 to 1 -> n | 1
Unset rightmost set bit:  turn the 1st rightmost 1 into 0 -> n & (n-1)

class Main {
    public static void main(String[] args) {
        int n = 18;

        // Set rightmost unset bit
        int setResult = n | (n + 1);
         
        // Set rightmost (lsb)set bit
        int setResult = n | 1;

        // Unset rightmost unset bit
        int unsetResult = n & (n + 1);

        System.out.println(n);
        System.out.println("After setting rightmost unset bit: " + setResult);
        System.out.println("After unsetting rightmost unset bit: " + unsetResult);
    }
}
