T.C : O(N/2) -> O(N)
S.C :  O(N/2) -> O(N)

Approach 1: using left and right var

class Main {
    static int revarr(int l,int r,int[] arr)
    {
        if(l==r)
         return arr[l];
        //swap(arr[l],arr[r]);
         int temp=arr[l];
         arr[l]=arr[r];
         arr[r]=temp;
         return revarr(l+1,r-1,arr);
    }
    public static void main(String[] args) {
     int n=5;
     int[] arr={1,2,3,4,5};
     revarr(0,n-1,arr);
     for(int i=0;i<n;i++)
     {
         System.out.print(arr[i]+" ");
     }
    }
}

Approach 2: without using var

class Main {
    static int revarr(int i,int n,int[] arr)
    {
        if(i>=n/2)
         return arr[i];
        //swap(arr[l],arr[r]);
         int temp=arr[i];
         arr[i]=arr[n-i-1];
         arr[n-i-1]=temp;
         
         return revarr(i+1,n,arr);
    }
    public static void main(String[] args) {
     int n=5;
     int[] arr={1,2,3,4,5};
     revarr(0,n,arr);
     for(int i=0;i<n;i++)
     {
         System.out.print(arr[i]+" ");
     }
    }
}

