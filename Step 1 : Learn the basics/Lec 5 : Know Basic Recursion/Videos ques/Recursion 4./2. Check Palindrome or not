T.C : O(n/2) ≈ O(n).
S.C : O(n/2) ≈ O(n).

class Main {
    static boolean palindrome(int i,int n,char[] arr)
    {
        if(i>=n/2)
        return true;
        if(arr[i]!=arr[n-i-1])
         return false;
      return palindrome(i+1,n,arr);
    }
    public static void main(String[] args) {
     int n=5;
     String a="MADAM";
     System.out.println(palindrome(0,n,a.toCharArray()));
    }
}
