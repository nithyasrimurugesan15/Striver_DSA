https://leetcode.com/problems/binary-tree-level-order-traversal/description/

Level order traversal runs in O(n) time and O(n) space because each node is processed once and the queue can store up to O(n) nodes in the worst case.

class Solution {
    public List<List<Integer>> levelOrder(TreeNode root) {
        Queue<TreeNode> q = new LinkedList<>();
        List<List<Integer>> res= new ArrayList<>();
        if(root == null ) {
            return res;
        }
        q.offer(root);
        while(!q.isEmpty()) {
            List<Integer> sublist = new ArrayList<>();
            int siz=q.size();
            for(int i=0;i<siz;i++) {
            TreeNode node = q.poll();
            sublist.add(node.val);

            if(node.left!=null) {
                q.add(node.left);
            }
            if(node.right!=null) {
                q.add(node.right);
            }

            }
            res.add(sublist);
        }

         return res;

    }
}
