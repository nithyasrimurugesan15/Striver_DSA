Time Complexity: O(V + E)
Space Complexity: O(V)

“DFS uses recursion, so in the worst case the recursion stack can grow up to V nodes.”

class Solution {
    public ArrayList<Integer> dfs(ArrayList<ArrayList<Integer>> adj) {
        ArrayList<Integer> res = new ArrayList<>();
        int s = adj.size();
        boolean[] visited = new boolean[s];
        dfs(0,adj,visited,res);
        return res;
        
    }
    public void dfs(int node, ArrayList<ArrayList<Integer>> adj,
    boolean[] visited , ArrayList<Integer> res)
    {
        
        visited[node] = true;
        res.add(node);
        
        for(int ele : adj.get(node)) {
            if(!visited[ele]) {
                dfs(ele,adj,visited,res);
            }
        }
        
    }
}
