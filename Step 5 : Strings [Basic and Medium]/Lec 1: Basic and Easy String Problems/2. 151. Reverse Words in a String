https://leetcode.com/problems/reverse-words-in-a-string/description/

Refer -> next lec 2 -> 8th ques

time : o(n) and space o(n)

class Solution {
    public String reverseWords(String s) {
        Stack<String> n=new Stack<>();
        s=s.trim()+" ";
        String st="";
        for(int i=0;i<s.length();i++)
        {
            if(s.charAt(i)==' ')
            {
                if(!st.isEmpty())
                {
                  n.push(st);
                  st="";
                }
            }
            else
            {
                st+=s.charAt(i);
            }
        }
        String ans="";
        while(n.size()!=1)
        {
            ans=ans+n.pop()+" ";
        }
        ans+=n.pop();
        return ans;
    }
}


class Solution {
    public String reverseWords(String s) 
    {
        // s=s.trim()+" ";
        String result="";
        String res="";
        Stack<String> st=new Stack<>();
        for(int i=0;i<s.length();i++)
        {
            char c=s.charAt(i);
            if(c!=' ')
            {
                res+=c;
            }
            else if(c==' ')
            {
                if(res.length()!=0)
                {
                    st.push(res);
                    res="";
                }
                else
                {
                    continue;
                }
            }

        }
        if(res.length()!=0)
        {
            st.push(res);
        }

        while(st.size()>1)
        {
            result=result+st.pop()+" ";
        }
        result+=st.pop();
        return result;    
    }
}


Appracoh optimal  : two pointer
