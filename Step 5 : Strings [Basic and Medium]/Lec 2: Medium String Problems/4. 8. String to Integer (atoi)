https://leetcode.com/problems/string-to-integer-atoi/

time :o(n) 

Space = O(1) (no recursion stack)

Space:

O(n) (because of recursion stack)
Even though the algorithm is logically O(1) space, recursion adds stack frames.
Worst case:
If the string has n digit characters, the recursion depth becomes n.
So recursion stack = O(n).


class Solution {
    public int myAtoi(String s) {
        s=s.trim();
        if(s.isEmpty())
        {
            return 0;
        }
       int i=0;
        int sign=1;
        if(s.charAt(i)=='-' || s.charAt(i)=='+')
        {
            sign=(s.charAt(i)=='-') ? -1 : 1;
            i++;
        }
        return helper(s,i,0,sign);
    }
    public int helper(String s,int i,int num,int sign)
    {
        if(i>=s.length() || !Character.isDigit(s.charAt(i)))
        {
            return num*sign;
        }
        int digit=s.charAt(i)-'0';
        if(num>(Integer.MAX_VALUE - digit)/10)
        {
            return (sign==1)? Integer.MAX_VALUE : Integer.MIN_VALUE;
        }
        num=num*10+digit;
        return helper(s,i+1,num,sign);
    }
}
