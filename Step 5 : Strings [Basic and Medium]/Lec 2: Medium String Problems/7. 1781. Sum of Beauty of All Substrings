https://leetcode.com/problems/sum-of-beauty-of-all-substrings/description/

o(n^2) and o(1)

class Solution {
    public int getmaxcount(int[] freq)
    {
        int max=Integer.MIN_VALUE;
        for(int i=0;i<26;i++)
        {
            max=Math.max(max,freq[i]);
        }
        return max;
    }

     public int getmincount(int[] freq)
    {
        int min=Integer.MAX_VALUE;
        for(int i=0;i<26;i++)
        {
            if(freq[i]!=0)
            {
               min=Math.min(min,freq[i]);
            }
        }
        return min;
    }
    
    public int beautySum(String s)
    {
        int sum=0;
        int[] freq=new int[26];
        for(int i=0;i<s.length();i++)
        {
             Arrays.fill(freq,0);   
            for(int j=i;j<s.length();j++)
            {
                freq[s.charAt(j)-'a']++;
                int beauty=getmaxcount(freq)-getmincount(freq);
                sum+=beauty;
            }
        }
        return sum;
    }
}



prefer: -> without using sep fun to find min and max -> speed inc -> beats 98.0
        -> with using na -> beats 34.0


time : O(n² * 26) = O(n²) 
space : O(26) = O(1)

class Solution {
    public int beautySum(String s)
    {
        int sum=0;
        int[] freq=new int[26];
        for(int i=0;i<s.length();i++)
        {
             Arrays.fill(freq,0);
             int max=0; 
            for(int j=i;j<s.length();j++)
            { 
                
                int ch=s.charAt(j)-'a';
                freq[ch]++;
                max=Math.max(max,freq[ch]);
                int min=Integer.MAX_VALUE;
                for(int k=0;k<26;k++)
                {
                     if(freq[k]!=0)
                    {
                       min=Math.min(min,freq[k]);
                    }
                }
                sum+=(max-min);
            }
        }
        return sum;
    }
}
